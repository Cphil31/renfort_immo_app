<form method="post" action="{{ path('app_statut_entreprise_delete', {'id': statut_entreprise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ statut_entreprise.id) }}">
    <button class="btn">Delete</button>
</form>
