{% extends 'base.html.twig' %}

{% block title %}StatutEntreprise{% endblock %}

{% block body %}
    <h1>StatutEntreprise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut_entreprise.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ statut_entreprise.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statut_entreprise_index') }}">back to list</a>

    <a href="{{ path('app_statut_entreprise_edit', {'id': statut_entreprise.id}) }}">edit</a>

    {{ include('statut_entreprise/_delete_form.html.twig') }}
{% endblock %}
