{% extends 'base.html.twig' %}

{% block title %}StatutAdresse{% endblock %}

{% block body %}
    <h1>StatutAdresse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut_adresse.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ statut_adresse.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statut_adresse_index') }}">back to list</a>

    <a href="{{ path('app_statut_adresse_edit', {'id': statut_adresse.id}) }}">edit</a>

    {{ include('statut_adresse/_delete_form.html.twig') }}
{% endblock %}
