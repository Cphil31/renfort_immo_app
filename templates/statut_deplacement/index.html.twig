{% extends 'base.html.twig' %}

{% block title %}StatutDeplacement index{% endblock %}

{% block body %}
    <h1>StatutDeplacement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Statut</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for statut_deplacement in statut_deplacements %}
            <tr>
                <td>{{ statut_deplacement.id }}</td>
                <td>{{ statut_deplacement.statut }}</td>
                <td>
                    <a href="{{ path('app_statut_deplacement_show', {'id': statut_deplacement.id}) }}">show</a>
                    <a href="{{ path('app_statut_deplacement_edit', {'id': statut_deplacement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_statut_deplacement_new') }}">Create new</a>
{% endblock %}
