{% extends 'base.html.twig' %}

{% block title %}StatutDeplacement{% endblock %}

{% block body %}
    <h1>StatutDeplacement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut_deplacement.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ statut_deplacement.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statut_deplacement_index') }}">back to list</a>

    <a href="{{ path('app_statut_deplacement_edit', {'id': statut_deplacement.id}) }}">edit</a>

    {{ include('statut_deplacement/_delete_form.html.twig') }}
{% endblock %}
