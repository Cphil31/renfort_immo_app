<!-- Button trigger modal -->
<button type="button" class="btn btn-primary btn-sm" data-bs-toggle="modal" data-bs-target="#newContactModal">
  Ajouter contact
</button>

<!-- Modal -->
<div class="modal fade" id="newContactModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5" id="exampleModalLabel">Ajouter contact</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
       {{ form_start(formNewContact) }}
        {{ form_label(formNewContact.statut, 'Statut') }}
        {{ form_widget(formNewContact.statut, {'attr': {'class': 'form-select'}}) }}
        {{ form_label(formNewContact.entreprise, 'Entreprise') }}
        {{ form_widget(formNewContact.entreprise, {'attr': {'class': 'form-control'}}) }}
        {{ form_label(formNewContact.poste, 'Poste') }}
        {{ form_widget(formNewContact.poste, {'attr': {'class': 'form-control'}}) }}
        {{ form_label(formNewContact.nom, 'Nom') }}
        {{ form_widget(formNewContact.nom, {'attr': {'class': 'form-control'}}) }}
        {{ form_label(formNewContact.prenom, 'Prénom') }}
        {{ form_widget(formNewContact.prenom, {'attr': {'class': 'form-control'}}) }}
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">fermer</button>
        <button type="button submit" class="btn btn-primary">Ajouter</button>
      </div>
       {{ form_end(formNewContact) }}
    </div>
  </div>
</div>