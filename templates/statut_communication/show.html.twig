{% extends 'base.html.twig' %}

{% block title %}StatutCommunication{% endblock %}

{% block body %}
    <h1>StatutCommunication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut_communication.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ statut_communication.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statut_communication_index') }}">back to list</a>

    <a href="{{ path('app_statut_communication_edit', {'id': statut_communication.id}) }}">edit</a>

    {{ include('statut_communication/_delete_form.html.twig') }}
{% endblock %}
