{% extends 'base.html.twig' %}

{% block title %}Communication{% endblock %}

{% block body %}
    <h1>Communication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ communication.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ communication.description }}</td>
            </tr>
            <tr>
                <th>Forfait</th>
                <td>{{ communication.forfait }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_communication_index') }}">back to list</a>

    <a href="{{ path('app_communication_edit', {'id': communication.id}) }}">edit</a>

    {{ include('communication/_delete_form.html.twig') }}
{% endblock %}
