{% extends 'base.html.twig' %}

{% block title %}Accompte{% endblock %}

{% block body %}
    <h1>Accompte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ accompte.id }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ accompte.prix }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ accompte.date ? accompte.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Payer</th>
                <td>{{ accompte.payer ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_accompte_index') }}">back to list</a>

    <a href="{{ path('app_accompte_edit', {'id': accompte.id}) }}">edit</a>

    {{ include('accompte/_delete_form.html.twig') }}
{% endblock %}
