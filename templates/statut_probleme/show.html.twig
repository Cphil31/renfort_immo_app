{% extends 'base.html.twig' %}

{% block title %}StatutProbleme{% endblock %}

{% block body %}
    <h1>StatutProbleme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut_probleme.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ statut_probleme.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statut_probleme_index') }}">back to list</a>

    <a href="{{ path('app_statut_probleme_edit', {'id': statut_probleme.id}) }}">edit</a>

    {{ include('statut_probleme/_delete_form.html.twig') }}
{% endblock %}
