<form method="post" action="{{ path('app_statut_contact_delete', {'id': statut_contact.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ statut_contact.id) }}">
    <button class="btn">Delete</button>
</form>
