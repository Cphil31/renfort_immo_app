{% extends 'base.html.twig' %}

{% block title %}StatutContact{% endblock %}

{% block body %}
    <h1>StatutContact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut_contact.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ statut_contact.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statut_contact_index') }}">back to list</a>

    <a href="{{ path('app_statut_contact_edit', {'id': statut_contact.id}) }}">edit</a>

    {{ include('statut_contact/_delete_form.html.twig') }}
{% endblock %}
