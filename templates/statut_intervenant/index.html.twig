{% extends 'base.html.twig' %}

{% block title %}StatutIntervenant index{% endblock %}

{% block body %}
    <h1>StatutIntervenant index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Statut</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for statut_intervenant in statut_intervenants %}
            <tr>
                <td>{{ statut_intervenant.id }}</td>
                <td>{{ statut_intervenant.statut }}</td>
                <td>
                    <a href="{{ path('app_statut_intervenant_show', {'id': statut_intervenant.id}) }}">show</a>
                    <a href="{{ path('app_statut_intervenant_edit', {'id': statut_intervenant.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_statut_intervenant_new') }}">Create new</a>
{% endblock %}
