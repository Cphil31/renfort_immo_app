<form method="post" action="{{ path('app_statut_intervenant_delete', {'id': statut_intervenant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ statut_intervenant.id) }}">
    <button class="btn">Delete</button>
</form>
