{% extends 'base.html.twig' %}

{% block title %}StatutIntervenant{% endblock %}

{% block body %}
    <h1>StatutIntervenant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut_intervenant.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ statut_intervenant.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statut_intervenant_index') }}">back to list</a>

    <a href="{{ path('app_statut_intervenant_edit', {'id': statut_intervenant.id}) }}">edit</a>

    {{ include('statut_intervenant/_delete_form.html.twig') }}
{% endblock %}
