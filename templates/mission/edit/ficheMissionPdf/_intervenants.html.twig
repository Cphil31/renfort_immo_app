
<p>
    Intervenants :
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Intervenants</th>
                <th scope="col">Nom</th>
                <th scope="col">Prenom</th>
                <th scope="col">Réalisations</th>
            </tr>
        </thead>
        <tbody>
            {% if intervenants %}

            {% for i in intervenants %}
            <tr>
                <th scope="row">{{ i.date ? i.date | date( "d/m/Y") }}</th>
                <td>{{ i.contact.nom }}</td>
                <td>{{ i.contact.prenom }}</td>
                <td>{{ i.realisations ? " , " ~ i.realisations : " " }}</td>
            </tr>
            {% endfor %}

            {% endif %}
        </tbody>
    </table>
</p>