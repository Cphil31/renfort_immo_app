{% extends 'base.html.twig' %}

{% block title %}DevisDeplacement{% endblock %}

{% block body %}
    <h1>DevisDeplacement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ devis_deplacement.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ devis_deplacement.quantite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ devis_deplacement.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_devis_deplacement_index') }}">back to list</a>

    <a href="{{ path('app_devis_deplacement_edit', {'id': devis_deplacement.id}) }}">edit</a>

    {{ include('devis_deplacement/_delete_form.html.twig') }}
{% endblock %}
