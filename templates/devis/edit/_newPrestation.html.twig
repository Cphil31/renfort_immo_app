<!-- Button trigger modal -->
<button type="button" class="btn btn-primary btn-sm" data-bs-toggle="modal" data-bs-target="#newPrestationModal">
  Ajouter Prestation
</button>

<!-- Modal -->
<div class="modal fade" id="newPrestationModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5" id="exampleModalLabel"> Prestation</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        
          {{ form_start(formPrestation) }}
            {{ form_label(formPrestation.quantite, 'Quantité') }}
            {{ form_widget(formPrestation.quantite, {'attr': {'class': 'form-control'}}) }}
            {{ form_label(formPrestation.prix, 'Prix') }}
            {{ form_widget(formPrestation.prix, {'attr': {'class': 'form-control'}}) }}

          
            
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">fermer</button>
          <button type="button submit" class="btn btn-primary btn-sm">Ajouter</button>
        </div>
          {{ form_end(formPrestation) }}
    </div>
  </div>
</div>