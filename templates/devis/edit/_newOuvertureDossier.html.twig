<!-- Button trigger modal -->
<button type="button" class="btn btn-primary btn-sm" data-bs-toggle="modal" data-bs-target="#newDossierModal">
  Ouverture de dossier
</button>

<!-- Modal -->
<div class="modal fade" id="newDossierModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5" id="exampleModalLabel"> Ouverture de dossier</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        
          {{ form_start(formOuverture) }}
            {{ form_label(formOuverture.quantite, 'Quantité') }}
            {{ form_widget(formOuverture.quantite, {'attr': {'class': 'form-control'}}) }}
            {{ form_label(formOuverture.prix, 'Prix unitaire') }}
            {{ form_widget(formOuverture.prix, {'attr': {'class': 'form-control'}}) }}
            {{ form_label(formOuverture.payer, 'payer ') }}
            {{ form_widget(formOuverture.payer, {'attr': {'class': ''}}) }}
        
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">fermer</button>
          <button type="button submit" class="btn btn-primary btn-sm">Ajouter</button>
        </div>
          {{ form_end(formOuverture) }}
    </div>
  </div>
</div>