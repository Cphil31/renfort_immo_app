{% extends 'base.html.twig' %}

{% block title %}{{ devi.name }}{% endblock %}

{% block body %}
<h1>Devis : {{ devi.name | capitalize}}</h1>
<h5> Mission : <a href={{ path('app_mission_show' , { 'id' : devi.mission.id})}}>{{devi.mission | capitalize}} </a></h5>
<h5>
    Produit :
    <a href={{ path('app_mission_show' , { 'id' : devi.mission.id})}}>
        {{devi.mission.produit.name | capitalize}}
    </a>
</h5>

<a href="{{ path('app_devis_index') }}">back to list</a>

<a href="{{ path('app_devis_edit', {'id': devi.id}) }}">edit</a>

{{ include('devis/_delete_form.html.twig') }}
{{ include('devis/show/_newProbleme.html.twig') }}
{{ include('devis/show/_ProblemeList.html.twig') }}
{% endblock %}