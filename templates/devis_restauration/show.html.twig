{% extends 'base.html.twig' %}

{% block title %}DevisRestauration{% endblock %}

{% block body %}
    <h1>DevisRestauration</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ devis_restauration.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ devis_restauration.quantite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ devis_restauration.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_devis_restauration_index') }}">back to list</a>

    <a href="{{ path('app_devis_restauration_edit', {'id': devis_restauration.id}) }}">edit</a>

    {{ include('devis_restauration/_delete_form.html.twig') }}
{% endblock %}
