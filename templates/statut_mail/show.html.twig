{% extends 'base.html.twig' %}

{% block title %}StatutMail{% endblock %}

{% block body %}
    <h1>StatutMail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut_mail.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ statut_mail.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statut_mail_index') }}">back to list</a>

    <a href="{{ path('app_statut_mail_edit', {'id': statut_mail.id}) }}">edit</a>

    {{ include('statut_mail/_delete_form.html.twig') }}
{% endblock %}
