{% extends 'base.html.twig' %}

{% block title %}Intervenant{% endblock %}

{% block body %}
    <h1>Intervenant</h1>
    <table class="table">
        <tbody>
            
            <tr>
                <th>Nom</th>
                <td>{{ intervenant.contact.nom }}</td>
            </tr>
              <tr>
                <th>Prénom</th>
                <td>{{ intervenant.contact.prenom }}</td>
            </tr>
                 <tr>
                <th>statut</th>
                <td></td>
            </tr>
              </tr>
                 <tr>
                <th>produit</th>
                <td>{{ intervenant.produit }}</td>
            </tr>
            <tr>
                <th>Observation</th>
                <td>{{ intervenant.observation }}</td>
            </tr>
            <tr>
                <th>Cout</th>
                <td>{{ intervenant.cout }}</td>
            </tr>
            <tr>
                <th>Durée</th>
                <td>{{ intervenant.duree }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ intervenant.date ? intervenant.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Realisations</th>
                <td>{{ intervenant.realisations }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_intervenant_index') }}">back to list</a>

    <a href="{{ path('app_intervenant_edit', {'id': intervenant.id}) }}">edit</a>

    {{ include('intervenant/_delete_form.html.twig') }}
{% endblock %}
