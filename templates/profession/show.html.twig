{% extends 'base.html.twig' %}

{% block title %}Profession{% endblock %}

{% block body %}
    <h1>Profession</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profession.id }}</td>
            </tr>
            <tr>
                <th>Profession</th>
                <td>{{ profession.profession }}</td>
            </tr>
            <tr>
                <th>Poste</th>
                <td>{{ profession.poste }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_profession_index') }}">back to list</a>

    <a href="{{ path('app_profession_edit', {'id': profession.id}) }}">edit</a>

    {{ include('profession/_delete_form.html.twig') }}
{% endblock %}
