<form method="post" action="{{ path('app_etat_des_lieux_delete', {'id': etat_des_lieux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etat_des_lieux.id) }}">
    <button class="btn">Delete</button>
</form>
