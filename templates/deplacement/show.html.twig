{% extends 'base.html.twig' %}

{% block title %}Deplacement{% endblock %}

{% block body %}
    <h1>Deplacement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ deplacement.id }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ deplacement.lieu }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ deplacement.date ? deplacement.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ deplacement.duree ? deplacement.duree|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Cout</th>
                <td>{{ deplacement.cout }}</td>
            </tr>
            <tr>
                <th>CoutPeage</th>
                <td>{{ deplacement.coutPeage }}</td>
            </tr>
            <tr>
                <th>CoutCarburant</th>
                <td>{{ deplacement.coutCarburant }}</td>
            </tr>
            <tr>
                <th>Observation</th>
                <td>{{ deplacement.observation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_deplacement_index') }}">back to list</a>

    <a href="{{ path('app_deplacement_edit', {'id': deplacement.id}) }}">edit</a>

    {{ include('deplacement/_delete_form.html.twig') }}
{% endblock %}
