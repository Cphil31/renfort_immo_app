{% extends 'base.html.twig' %}

{% block title %}OuvertureDossier{% endblock %}

{% block body %}
    <h1>OuvertureDossier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ouverture_dossier.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ ouverture_dossier.quantite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ ouverture_dossier.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ouverture_dossier_index') }}">back to list</a>

    <a href="{{ path('app_ouverture_dossier_edit', {'id': ouverture_dossier.id}) }}">edit</a>

    {{ include('ouverture_dossier/_delete_form.html.twig') }}
{% endblock %}
