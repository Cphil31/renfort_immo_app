{% extends 'base.html.twig' %}

{% block title %}DevisReunion index{% endblock %}

{% block body %}
    <h1>DevisReunion index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantite</th>
                <th>Prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for devis_reunion in devis_reunions %}
            <tr>
                <td>{{ devis_reunion.id }}</td>
                <td>{{ devis_reunion.quantite }}</td>
                <td>{{ devis_reunion.prix }}</td>
                <td>
                    <a href="{{ path('app_devis_reunion_show', {'id': devis_reunion.id}) }}">show</a>
                    <a href="{{ path('app_devis_reunion_edit', {'id': devis_reunion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_devis_reunion_new') }}">Create new</a>
{% endblock %}
