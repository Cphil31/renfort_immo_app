<form method="post" action="{{ path('app_devis_reunion_delete', {'id': devis_reunion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devis_reunion.id) }}">
    <button class="btn">Delete</button>
</form>
