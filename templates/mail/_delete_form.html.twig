<form method="post" action="{{ path('app_mail_delete', {'id': mail.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer ce mail ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mail.id) }}">
    <button class="btn btn-danger btn-sm">supprimer</button>
</form>
